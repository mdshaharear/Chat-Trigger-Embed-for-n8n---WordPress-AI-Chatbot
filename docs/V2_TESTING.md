# Phase 8 Testing

Minimum checks for the alpha branch:

* Baseline build and lint
* PHP lint
* Unit tests
* Migration compatibility tests
* Secret-masking checks
* Native UI initialization checks
* Mock provider contract tests

Real provider tests require explicit administrator confirmation in a real WordPress environment.

